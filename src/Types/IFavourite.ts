export interface IFavourite {
  productId: number;
  title: string;
  summary: string;
  image: string;
}

export enum colorFavourite {
  action = "action",
  primary = "primary",
  secondary = "secondary",
}
